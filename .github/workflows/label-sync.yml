name: Create and Sync PR Based on Label

on:
  pull_request:
    types: [labeled, synchronize]

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-pr:
    runs-on: ubuntu-latest
    if: github.event.pull_request.state == 'open'

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      #########################################################
      # Detect label (dev or stage)
      #########################################################
      - name: Detect Label
        id: detect_label
        if: github.event.action == 'labeled'
        run: echo "label=${{ github.event.label.name }}" >> $GITHUB_OUTPUT

      #########################################################
      # Choose target branch
      #########################################################
      - name: Determine Target Branch
        id: target
        if: github.event.action == 'labeled'
        run: |
          if [ "${{ steps.detect_label.outputs.label }}" = "dev" ]; then
            echo "branch=dev" >> $GITHUB_OUTPUT
          elif [ "${{ steps.detect_label.outputs.label }}" = "stage" ]; then
            echo "branch=stage" >> $GITHUB_OUTPUT
          else
            echo "branch=" >> $GITHUB_OUTPUT
          fi

      - name: Stop if invalid label
        if: github.event.action == 'labeled' && steps.target.outputs.branch == ''
        run: echo "Invalid label — no sync created."

      #########################################################
      # Create Sync PR (using PAT)
      #########################################################
      - name: Create Sync PR
        id: create_sync
        if: github.event.action == 'labeled' && steps.target.outputs.branch != ''
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.MY_PAT }}
          title: "Sync: ${{ github.event.pull_request.head.ref }} → ${{ steps.target.outputs.branch }}"
          body: |
            🔄 Auto-generated **Sync PR**

            **Main PR:** #${{ github.event.pull_request.number }}
            **Feature Branch:** `${{ github.event.pull_request.head.ref }}`
            **Target Branch:** `${{ steps.target.outputs.branch }}`

          base: ${{ steps.target.outputs.branch }}
          branch: "sync/${{ github.event.pull_request.number }}-${{ steps.target.outputs.branch }}"
          delete-branch: false

      #########################################################
      # Add comment on MAIN PR with Sync PR link
      #########################################################
      - name: Comment Sync PR Link
        if: github.event.action == 'labeled' && steps.target.outputs.branch != ''
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.MY_PAT }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ### 🔄 Sync PR Created!
            **Target branch:** `${{ steps.target.outputs.branch }}`
            👉 **Sync PR:** ${{ steps.create_sync.outputs.pull-request-url }}

            Merge this Sync PR to update `${{ steps.target.outputs.branch }}`.

      #########################################################
      # Auto-sync on new commits
      #########################################################
      - name: Auto-sync when commits update
        if: github.event.action == 'synchronize'
        env:
          TOKEN: ${{ secrets.MY_PAT }}
        run: |
          TARGETS=("dev" "stage")
          for target in "${TARGETS[@]}"; do
            SYNC_BRANCH="sync/${{ github.event.pull_request.number }}-$target"

            if git ls-remote --exit-code origin "$SYNC_BRANCH"; then
              echo "Updating sync branch: $SYNC_BRANCH"
              git push https://x-access-token:$TOKEN@github.com/${{ github.repository }} HEAD:$SYNC_BRANCH -f
            else
              echo "No sync branch for $target"
            fi
          done
